---
title: Testing Strategy Overview
description: A testing level escalation guide for AI-assisted frontend development.
sidebar_position: 1
---

## Why Testing Levels Matter

When an AI agent fixes a bug and declares it resolved, the fix is only as reliable as the testing method used to verify it. A unit test can confirm logic is correct, but it cannot confirm that the user actually sees the result on screen. This gap between "logically correct" and "visually correct" is the most common source of false confidence in AI-assisted development.

This guide defines **five testing levels**, each with increasing coverage and decreasing blind spots. The core principle is simple:

<Note>
Match your testing level to what you are actually verifying. If you are checking logic, Level 1 is sufficient. If you are checking whether something is visible on screen, you need Level 5.
</Note>

## Who This Guide Is For

This guide serves two audiences:

- **AI agents** (Claude Code, Copilot, etc.) that need to choose the right testing approach when verifying their own work
- **Developers** who work with AI agents and want to understand the testing escalation ladder

## The Escalation Principle

Testing should escalate, not repeat. When a lower-level test passes but the problem persists, move to the next level:

1. **Level 1** -- Unit/Logic Tests (vitest, jest)
2. **Level 2** -- DOM-based Component Tests (jsdom, Testing Library)
3. **Level 3** -- Build Output Verification (read built files)
4. **Level 4** -- E2E Browser Tests (Playwright, headless browser)
5. **Level 5** -- Deterministic + Visual Verification (verify-ui + headless browser)

Each level covers blind spots that lower levels cannot detect. The common failure pattern is staying at Level 1 when the bug requires Level 4 or 5.

## How to Use This Guide

1. Start with the [Decision Guide](/pj/zudo-test-wisdom/docs/decision-guide) to determine which level fits your current task
2. Read the [Testing Levels](/pj/zudo-test-wisdom/docs/testing-levels) section for detailed coverage of each level
3. Reference [Real-World Patterns](/pj/zudo-test-wisdom/docs/real-world-patterns) for production-tested configurations
4. Keep the [Tools Reference](/pj/zudo-test-wisdom/docs/tools-reference) open for quick lookup
