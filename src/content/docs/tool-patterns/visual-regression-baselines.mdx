---
title: Deterministic Visual Regression with Committed Baselines
description: Playwright toHaveScreenshot() with committed baseline PNGs -- the deterministic pixel-diff gate the Level 5 skill set is missing, plus the determinism and tolerance discipline that keep it honest.
sidebar_position: 3
---

[Level 5](../testing-levels/level-5-visual-verification.mdx) pairs deterministic computed-style assertions with an _informal_ screenshot review -- explicitly "not a repeatable regression gate," because the guide's current skill set has no pixel-diff tool. That leaves a real gap: nothing in the taxonomy catches a purely _visual_ regression -- a shifted gradient, a recolored SVG, a canvas frame rendered a pixel off -- as a deterministic pass/fail signal. This page fills it. Playwright's `toHaveScreenshot()` compares a freshly rendered screenshot against a **committed baseline PNG** and fails the run when they diverge beyond a set tolerance. It is the deterministic _screenshot half_ of the Level 5 family: the same "what a test can see" as an informal look, but wired as a gate instead of a sanity glance.

The catch is that a pixel gate is only as trustworthy as its determinism. An un-engineered screenshot test is among the most common sources of false-red flake in a browser suite, and the reflex fix -- loosen the tolerance until it passes -- is exactly the [gate-gaming this guide forbids](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate). Most of this page is about earning the determinism that lets the gate stay strict.

## How `toHaveScreenshot()` Works

The mechanism is a committed reference image. A spec renders a locator and compares it against a PNG stored next to the test:

```typescript
// e2e/visual/hero.spec.ts
import { test, expect } from "@playwright/test";

test("hero section matches its baseline", async ({ page }) => {
  await page.goto("/");
  await expect(page.locator(".hero")).toHaveScreenshot("hero.png", {
    maxDiffPixels: 100,
  });
});
```

The first run with no baseline writes `hero.png` into the snapshot directory and **fails** -- Playwright treats "no reference to compare against" as a failure, never a silent pass. (This is the same principle the [remark/rehype golden-fixture corpus](../project-recipes/remark-rehype-plugin-testing.mdx) applies to HTML: a run that finds no committed baseline fails hard rather than adopting whatever it just produced.) Every later run re-renders, diffs against the committed PNG, and passes only if the difference stays under the threshold.

There are two tolerance knobs plus a per-pixel sensitivity, and the difference between them matters:

| Option | Meaning | Use when |
|---|---|---|
| `maxDiffPixels` | absolute count of differing pixels allowed | a small fixed region where you can reason about an exact pixel budget |
| `maxDiffPixelRatio` | fraction (0–1) of differing pixels allowed | full-page or responsive shots, where an absolute count scales with the viewport |
| `threshold` | per-pixel color-distance sensitivity (0–1, default 0.2) | tuning how different a single pixel must be to count as "different" at all |

Set defaults once in the config so every spec inherits the same discipline:

```typescript
// playwright.config.ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  expect: {
    toHaveScreenshot: {
      maxDiffPixelRatio: 0.01, // ≤1% of pixels may differ
      threshold: 0.2, // per-pixel color-distance sensitivity (0–1)
      animations: "disabled", // freeze CSS animations/transitions for the shot
    },
  },
  snapshotPathTemplate:
    "e2e/visual/__screenshots__/{testFilePath}/{arg}-{projectName}-{platform}{ext}",
});
```

The `{projectName}` and `{platform}` tokens are load-bearing. Playwright keys baselines by project and OS by default, because the same page renders differently on `chromium-linux` and `webkit-darwin`. A baseline is only ever compared against a screenshot from the _same_ project and platform -- which is also why CI has to render on the same OS the baseline was captured on.

## Baselines Are Reviewed Like Code

The committed PNGs are artifacts of record, not throwaway cache. They live in the repo, they show up in the PR diff, and a reviewer looks at the rendered before/after exactly as they would read a code change. That single decision -- baselines in version control -- is what turns a screenshot comparison into a regression _gate_ rather than a snapshot that quietly rewrites itself.

Regeneration must therefore be an explicit, reviewable act, never an automatic one. Two disciplines enforce that:

- **Regenerate on demand, never during the normal run.** `npx playwright test --update-snapshots` rewrites baselines locally; a plain run only ever compares. A test that silently rewrites its own baseline on mismatch is not a gate -- it can never fail.
- **Gate regeneration behind an explicit switch on render-sensitive surfaces.** In pgen -- a GPU pattern generator whose PNG output shifts subtly with driver and antialiasing -- baseline capture runs only under an explicit `PGEN_REGEN_BASELINES` mode, adopted precisely because the render was stable enough to gate but not stable enough to recapture casually. zzmod carries a dedicated visual baseline-capture script for the same reason: capturing is a deliberate command, distinct from running the suite.

A surprising diff in the PR means an unintended regression; an expected diff _is_ the new baseline, and committing it is the reviewer accepting the new visual truth. This is the same review loop the golden-fixture corpus uses for HTML -- a changed reference is a reviewable event, not a silent side effect of running the tests.

<Warning>

"Just re-run with `--update-snapshots`" is the visual-regression equivalent of deleting a failing assertion. It is correct _only_ when you have already looked at the diff and decided the new rendering is right. Regenerating to make red turn green, without inspecting what changed, launders a regression into the baseline -- and every run after that passes against the corrupted reference.

</Warning>

## Engineering Determinism

A pixel gate fails the moment the same input renders two different images, so every non-deterministic source in the render has to be pinned before the first baseline is captured. The usual suspects:

- **Animations and transitions.** A shot taken mid-transition is a coin flip. Set `animations: "disabled"` (Playwright fast-forwards finite CSS animations and transitions to their end state and cancels infinite ones to their initial state) or force `prefers-reduced-motion` -- the same reduced-motion technique the [deflaking recipe](../test-integrity/deflaking-recipe.mdx) uses for its animation-related flakes.
- **Blinking carets and focus rings.** A text caret blinks; `toHaveScreenshot` hides it by default (`caret: "hide"`), but a custom cursor or a focus ring you draw yourself is not covered -- suppress those via injected CSS (`caret-color: transparent`) or mask the element.
- **Web-font loading.** A shot taken before the web font loads captures fallback-font metrics -- different glyph widths, different line wraps. Wait for `document.fonts.ready` before asserting.
- **Dynamic content.** Timestamps, random IDs, live data -- mask them with `toHaveScreenshot`'s `mask` option or stub them, so the only thing under comparison is the layout you actually mean to gate.
- **Viewport and device pinning.** Screenshot size is a function of viewport; pin `viewport` (and `deviceScaleFactor`) per project, and capture dark and light schemes as separate baselines via `colorScheme`.
- **GPU and antialiasing variance.** The hard one. Subpixel antialiasing and GPU rendering differ across drivers and runners -- the reason pgen needed a regen mode and a same-hardware capture policy at all. Where you cannot eliminate the variance, absorb _only that much_ of it with a threshold (next section) and pin the render to consistent hardware.

```typescript
// A determinism-hardened visual spec
test("card renders identically", async ({ page }) => {
  await page.goto("/cards/42");
  await page.evaluate(() => document.fonts.ready); // fonts settled
  await page.emulateMedia({ reducedMotion: "reduce" }); // no animation mid-frame
  await expect(page.locator(".card")).toHaveScreenshot("card.png", {
    mask: [page.locator(".timestamp")], // exclude volatile content
    maxDiffPixelRatio: 0.005,
  });
});
```

<Tip>

Capture the baseline in the _same environment that will run the gate_. A baseline captured on a developer's macOS machine and compared against a Linux CI render will diff on font hinting and antialiasing alone -- a guaranteed false red that has nothing to do with the product. This is why baselines are keyed by platform and why CI regenerates them on its own runner image.

</Tip>

## Tolerance Absorbs Noise, Never Regressions

A threshold exists for exactly one reason: to absorb rendering noise you have proven you cannot eliminate -- a few antialiased pixels along a GPU-drawn curve, sub-pixel font hinting on a platform you must support. It is a noise floor, not a regression allowance. The test is directional:

- **Legitimate:** you can name the physical noise source, you set the threshold just above it, and a real visual change still exceeds it and fails.
- **Illegitimate:** a real diff is failing, and you raise the threshold until it passes. That is [Rule 8 -- Never Game the Gate](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate). The guide names "loosened screenshot tolerance (e.g., raised pixel threshold)" as a specific instance of gaming the gate, on the same list as deleting an assertion or skipping a test.

<Danger>

Loosening `maxDiffPixels` / `maxDiffPixelRatio` / `threshold` to turn a red run green must be treated as suspect by default. Per the required-behavior rule, such an edit needs a linked issue and a **fresh-context review** of the diff -- it may not be applied in the same session that authored the change that caused the mismatch. A widened tolerance is indistinguishable, at the config level, from a real regression being waved through; only the review makes them distinguishable.

</Danger>

The healthy direction of travel is _tightening_: as you engineer more determinism (fonts, animation, hardware pinning), the noise floor drops and the threshold should drop with it. A threshold that only ever grows is a suite slowly going blind.

## Admitting a Baseline as an Oracle

Capturing a baseline and _admitting_ it as an oracle are two different acts. The capture is a file some command produced; the oracle is the claim that this file is what a correct build looks like. Everything above earns the determinism that makes such a claim possible -- none of it verifies that the claim actually holds for the specific bytes you are about to commit. A capture taken from a dirty tree, a half-finished build, or a stale artifact directory looks exactly like a good one in the PR diff, and it holds that false position for as long as nobody replays it.

Five rules close the gap between capture and oracle:

1. **Capture on a clean, pinned parent.** The baseline describes the _parent_ commit's output, so capture it from a clean checkout of the parent, with the environment pinned exactly as the gate will pin it. A capture taken on top of the topic's own changes cannot detect those changes.
2. **Independently replay the unchanged parent before accepting the baseline.** Build the parent a second time from a clean state and compare that output against the stored capture. A capture that cannot reproduce at the very commit it claims to describe is not an oracle -- it is a record of whatever happened to be in the working tree that day.
3. **Fail baseline admission on unexplained mismatch -- do not defer discovery to final validation.** The replay is a gate of its own, run at admission time. Deferring it means the entire topic branch gets built and reviewed against a reference nobody verified, and the mismatch finally surfaces at the worst possible moment: attached to the change, where it reads as a regression.
4. **When a bad capture is discovered late, triangulate before repairing.** Do not repair the contract from the failing diff alone -- run the three-way comparison below, and **retain the mismatch evidence**. Those diffs are the record that justifies whatever you then change about the oracle, in the same spirit as a [versioned evidence contract](../test-integrity/live-proof-evidence-contracts.mdx).
5. **Never rewrite or loosen the oracle until a fresh-context review has classified the discrepancy.** Regenerating a capture or widening what it accepts is the same class of act as [raising a pixel threshold](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate), and carries the same requirement: someone who did not author the change classifies the discrepancy first -- stale capture, wrong contract, or real regression.

<Note>

This protocol is not specific to screenshots. Any **captured build-output oracle** is admitted the same way: asset manifests, generated-code hashes, [built-site integrity snapshots](../project-recipes/site-integrity-checks.mdx), bundle-size records. In every case the capture is a file produced by a command, the oracle is the claim that the file describes a correct build, and only a replay closes the distance between the two. The file's existence proves nothing.

</Note>

### C/P/T Triangulation

When a captured oracle disagrees with a topic branch, the failing diff alone cannot say _which side_ is wrong. Triangulation answers that with three artifacts, all produced by **the same command under the same pinned environment**:

| Artifact | What it is |
|---|---|
| **C** | the stored capture -- the oracle under suspicion |
| **P** | a fresh clean replay of the _unchanged parent_ |
| **T** | a fresh clean build of the _topic_ |

The attribution rule falls straight out of the three:

```text
C != P                           -> the capture does not reproduce; C is stale
C -> T shows that same mismatch  -> C is not admissible evidence against the topic
P -> T                           -> the only comparison attributable to the change
```

If `C != P` **and** the `C -> T` comparison shows that same mismatch, the mismatch predates the topic entirely. **Only `P -> T` is attributable to the code change.** Preserve all three diffs before touching the contract: they are what separates "the oracle was wrong" from "the change was wrong," and a repaired contract with no retained evidence is indistinguishable from a quietly regenerated one.

**A worked case.** A temporary emitted-asset hash oracle, captured at the parent SHA, could not reproduce at that same SHA -- captured versus clean parent showed **14 removed and 14 added asset names**. The topic then showed the identical mismatch against the capture, which is the signature of a stale `C`, not of a broken topic. The direct clean-parent versus clean-topic comparison (`P -> T`) differed in exactly one intentional island hash, caused by five new changelog pages; CSS and every theme hash were unchanged.

Without the parent replay, that stale capture would have falsely blamed the implementation -- or, worse, invited a blind regeneration that laundered the unexplained 14/14 delta into the new reference. The three-way evidence located the fault in the **oracle contract** rather than the topic, and supported narrowing that contract to a no-new-failures check instead of an exact-name match it could not honestly hold.

## Toolchain Identity Is Part of the Baseline

For a baseline that records **exact compiled bytes** -- a wasm payload size, a gzipped bundle byte count, a hash of generated output -- the producing compiler and optimizer version belongs to the baseline's identity as surely as the OS belongs to a screenshot's. Playwright makes that explicit by keying snapshots with `{platform}`. A byte-exact size manifest usually has no such key, so the toolchain version drops silently out of the record and the number looks portable when it is not.

**A worked case.** A local audit ran on a floating `stable` Rust channel that resolved to **1.96**, and recorded gzip-9 outputs of **1,451,831 B** for the root bundle and **259,809 B** for the parser. The PR gate later resolved _the same floating channel_ to **Rust 1.98** and deterministically produced **1,442,270 B** and **260,760 B** (from final wasm of **3,293,554 B** and **645,272 B** respectively) -- one number down, one number up, no source change between them. The committed exact manifest still described the pre-migration artifacts, so CI **correctly failed**, even though every gzip payload stayed comfortably below its independent ceiling.

Nothing was wrong with the code, and nothing was wrong with the gate. What was wrong was a baseline whose identity omitted its producer. The rule:

> Either pin the toolchain identity, or accept that a floating channel makes remeasurement a reviewed, versioned event.

A floating channel is a legitimate choice; it only moves the cost. Pinned, the manifest stays valid until you deliberately bump the pin. Floating, every toolchain movement becomes a baseline-refresh event with a reviewer looking at the new numbers -- exactly the review loop a screenshot regeneration goes through.

### A Ceiling and an Exact-Byte Record Are Different Contracts

That case turns on a distinction worth stating on its own: the two assertion kinds have different admission rules, and collapsing them produces either a brittle gate or a blind one.

| Contract | Asserts | Under toolchain movement | On mismatch |
|---|---|---|---|
| **Size ceiling** | output stays under a budget you chose | stays valid -- a few kilobytes either way does not invalidate the budget | a real signal: something got materially bigger |
| **Exact-byte record** | output _is_ these bytes, as of this measurement | is invalidated -- a new compiler legitimately emits different bytes | check provenance first, then refresh as a reviewed event |

Keep both, and keep them separate. The ceiling is the **stable safety assertion** -- it is what actually protects users from a bundle-size regression, and it should not move because a compiler bump shifted a number by 0.7%. The exact-byte record is **informational and documentary** -- it makes drift visible and dates the measurement, and it is _expected_ to be refreshed under review whenever its producer changes. Refreshing the record is routine bookkeeping; raising the ceiling to turn a red run green is [gaming the gate](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate).

## Where This Layer Sits

This is a **deterministic Level 5-family gate** -- the same reproducible, no-LLM pass/fail character as computed-style assertions, aimed at the visual output instead of specific property values. It does not replace computed-style checks; the two cover different failure shapes.

| Situation | Reach for |
|---|---|
| One property must equal an exact value (`font-size: 48px`, `height` not `0px`) | Computed-style assertion ([Level 5](../testing-levels/level-5-visual-verification.mdx)) |
| Gradient, shadow, SVG recolor, canvas/GPU output -- visual truth with no single property to name | Committed-baseline pixel diff (this page) |
| Rich layout interplay too broad to enumerate as properties | Committed-baseline pixel diff |
| Churny content, heavy cross-platform font variance, text-dominated pages | Computed-style assertions -- a pixel diff will flake |
| No stable DOM rect, computed styles don't apply, pixel diff too noisy to be deterministic | [Level 6](../testing-levels/level-6-ai-based-verification.mdx) AI verdict -- final resort |

Computed styles answer "is this one value correct?" A baseline pixel diff answers "does the whole rendered region still look like the reference?" -- which is strictly more than any finite list of `toHaveCSS` assertions can express for a gradient or a canvas, and strictly worse for a page whose content legitimately changes every run. On surfaces whose _product_ is the rendered image -- SVG color-mapping panels (zpanels), layout-as-product dashboards (zudome) -- there is no single property standing in for "correct," and the committed-baseline diff is the only regression net that sees what the user sees.

The boundary with [Level 6](../testing-levels/level-6-ai-based-verification.mdx) is determinism. A committed-baseline diff is reproducible and belongs in CI; an L6 AI verdict is non-deterministic, cost-bearing, and explicitly not a CI gate. When the surface is a `<canvas>` with no stable rect _and_ the pixel diff is too noisy to trust -- the two conditions the guide requires together -- that is the narrow case that escalates past this layer to L6, not a reason to loosen a threshold here.

## CI Realities

- **Storage: in-repo vs artifacts.** Committed baselines are the reviewable default -- the diff _is_ the review. The cost is repository weight: PNGs are binary, they grow the pack, and a wide device/scheme/OS matrix multiplies fast. Keep baselines scoped to the surfaces that actually earn a pixel gate; do not baseline every page. Some teams store baselines as CI artifacts or in external storage instead -- you shed repo weight but lose the PR-diff review that makes the gate honest, so prefer in-repo until size forces the trade.
- **Per-OS baselines.** Because renders differ by platform, each OS you gate needs its own baseline set, captured on that OS. Generate them on the CI runner image, not a developer laptop, so the reference matches the gate's environment.
- **The update flow.** Locally: `npx playwright test --update-snapshots`, then **review the resulting PNG diff in the PR** before committing. The reviewable event is the changed baseline; a PR that updates baselines with no visual explanation is exactly the smell the [tolerance rule](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate) exists to catch.
- **GPU-bound baselines belong on a schedule.** When the render needs a real GPU that PR runners lack, the pixel gate cannot judge on every PR -- it is the canonical [heavy-test Case B](../decision-guide/heavy-test-decision.mdx). Run it on capable hardware in a [scheduled re-exam](../ci-operations/scheduled-re-exam.mdx) instead of demoting it to a lower level; the level is right, only the tier moves.

## Where to Go Next

- [Level 5: Deterministic + Visual Verification](../testing-levels/level-5-visual-verification.mdx) -- the computed-style + informal-screenshot layer this deterministic pixel gate completes
- [Level 6: AI-Based Visual Verification](../testing-levels/level-6-ai-based-verification.mdx) -- the non-deterministic final resort for surfaces even a pixel diff cannot judge
- [Required Behavior — Never Game the Gate](../decision-guide/required-behavior.mdx#the-four-execution-rules-rule-8-—-never-game-the-gate) -- why loosening a screenshot tolerance to pass is treated as gaming the gate
- [Flake Root-Cause Catalog & Deflaking Recipe](../test-integrity/deflaking-recipe.mdx) -- the animation and hydration determinism fixes a pixel gate depends on
- [Built-Site Integrity Gates](../project-recipes/site-integrity-checks.mdx) -- other captured build-output oracles that admit under the same replay protocol
- [Heavy Test Decision Rule](../decision-guide/heavy-test-decision.mdx) -- when a GPU-bound pixel gate moves to the scheduled tier
- [Scheduled Re-exam & Night Exam](../ci-operations/scheduled-re-exam.mdx) -- how a GPU visual-regression lane runs on real hardware off the PR path
