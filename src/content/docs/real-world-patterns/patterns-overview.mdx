---
title: Patterns Overview
description: Source projects, pattern categories, and a symptom-to-pattern lookup table for real-world testing patterns.
sidebar_position: 1
---

## Production-Tested Approaches

The patterns in this section come from real production projects, not theoretical best practices. Each pattern has been used in shipped software and refined through actual bugs and failures.

## Source Projects

| Project | Type | Key Testing Patterns |
|---------|------|---------------------|
| **zudo-text** | Tauri text editor (monorepo) | Vitest unit across workspace packages, Rust `cargo test` core crate, dual-browser Playwright (Chromium CI / WebKit local), mock backend adapter + console-error monitoring, tiered e2e tags |
| **zzmod** | Content website (zfb + Preact islands) | Vitest unit (jsdom, Testing Library/Preact, MSW), production-build Playwright, tag-based @smoke/@heavy lanes, CI image interception, heavy E2E off-CI via local b4push |
| **pgen** | Pattern generator (monorepo + GPU runtime) | Deterministic PNG rendering, Miniflare + D1/R2 worker integration, tag-tiered Playwright e2e, GPU visual-regression (SSIM drift, GLSL parity) |
| **mdx-formatter** | CLI + npm library (Rust napi) | Contract-testing the Rust engine via Vitest, idempotency invariant, snapshot regression vs committed baselines, CLI integration via execFile |
| **zfb** | Frontend build orchestrator / SSG | Rust workspace `cargo test` (primary), binding-shape stubs, emitted-code dynamic import, single-source-of-truth guard, happy-dom router quirks, node-free Docker smoke E2E |
| **zudo-doc** | Doc-site framework | 6-level test taxonomy (Vitest unit → static dist reads → Playwright multi-fixture E2E → verify-ui), remark/rehype golden-fixture corpus + mdast tree factories |

## Pattern Categories

### [Vitest Patterns](./vitest-patterns.mdx)

Workspace configurations, jsdom/happy-dom environments, contract testing, idempotency testing, and Miniflare integration testing.

### [Playwright Patterns](./playwright-patterns.mdx)

CI-safe test splitting, console error monitoring, image interception, production build verification, and sharded CI runs.

### [Tauri Testing](./tauri-testing.mdx)

Dual WebKit-first (local) + Chromium-CI testing strategy, core crate pattern, backend bridge mocking, and the full 8-step escalation ladder for desktop apps.

### [Backend & Node.js Testing](./backend-testing.mdx)

Cloudflare Functions with Miniflare, HTTP API testing, fetch mocking with `vi.stubGlobal`, file system testing with temp directories, and key principles for separating frontend and backend test configs.

### [Crypto & Hash Testing](./crypto-testing.mdx)

Known-answer (hardcoded-digest) crypto tests, cross-input-type equivalence (string vs `ArrayBuffer`), and catching encoding bugs that "deterministic" tests miss.

### [Testing remark/rehype Plugins](./remark-rehype-plugin-testing.mdx)

mdast tree factories for unit tests plus a golden-fixture corpus with `UPDATE_FIXTURES` regeneration and a cross-language parity oracle.

### [Flake Root-Cause Catalog & Deflaking Recipe](./deflaking-recipe.mdx)

The seven causes of E2E flakiness (timing waits, `networkidle` misuse, animations, shared state, hydration races, background-work starvation, lane over-subscription) and a five-step recipe -- four mechanical fixes plus an escalation step -- for eliminating each one.

### [Scheduled Re-exam & Night Exam](./scheduled-re-exam.mdx)

Operating heavy, platform-bound test lanes on a schedule — the CI re-exam workflow, deduped failure issue filing, on-demand pre-merge dispatch, and the local night exam with agent triage.

### [Browser Verification in Limited Environments](./web-container-browser.mdx)

Running browser-based verification (`/verify-ui`, `/headless-browser`) in sandboxed containers where the Playwright CDN is blocked — pre-installed Chromium fallback, `127.0.0.1` dev-server binding, and the Cloudflare Workers preview-URL path.

### [Migrating a Rust Suite to cargo-nextest](./cargo-nextest-migration.mdx)

Why the retry-budget, per-test-timeout, and flake-telemetry policy this guide requires is unimplementable on plain `cargo test`, and a mechanical checklist for migrating to cargo-nextest instead.

### [Publishing-Pipeline Verification](./publishing-pipeline.mdx)

Publish-time gates for platform-binary npm packages (esbuild-style meta package + per-platform `optionalDependencies`) — catching silent skips on unpropagated tarballs, stripped executable bits, and the double-PUT publish race that the nightly registry drift net cannot see.

### [Built-Site Integrity Gates](./site-integrity-checks.mdx)

Deterministic gates over the built site -- link check, html-validate, full-site crawl smoke, and 404/OGP/image-health sweeps -- for failures no unit or component test can structurally see.

### [Deterministic Visual Regression with Committed Baselines](./visual-regression-baselines.mdx)

Playwright `toHaveScreenshot()` with committed baseline PNGs -- the deterministic pixel-diff gate that completes Level 5's informal screenshot review, plus the determinism and tolerance discipline that keep it honest.

### [Environment-Tiered Testing Against Deployed Targets](./environment-tiered-testing.mdx)

One contract suite pointed at local, a preview deploy, and production -- what belongs in each tier, how to reach gated previews, and why a production failure is an incident, not a test run.

## Start from Your Symptom

Not sure which pattern applies? Start from what you are actually seeing:

| Symptom | Start here |
|---|---|
| A flaky E2E test -- passes most of the time, fails without a code change | [Flake Root-Cause Catalog & Deflaking Recipe](./deflaking-recipe.mdx) |
| A test reports green, but it never actually ran | [Playwright Patterns § Guard against specs that match no project](./playwright-patterns.mdx#guard-against-specs-that-match-no-project) / [the `test.skip` pass-by-skip trap](./playwright-patterns.mdx#test-skip-as-a-precondition-—-the-pass-by-skip-trap) |
| `EADDRINUSE` on a port nothing in your config claims to use | [Playwright Patterns § The Hidden Auxiliary-Socket Port Race](./playwright-patterns.mdx#the-hidden-auxiliary-socket-port-race) |
| Not sure whether to test against real D1 or a stub | [Backend & Node.js Testing § Mock the Binding, Not the Runtime](./backend-testing.mdx#mock-the-binding-not-the-runtime) |
| CI is green, but the published artifact is broken | [Publishing-Pipeline Verification](./publishing-pipeline.mdx) |
| A build that worked last month fails today with nothing in your own diff | [Scheduled Re-exam & Night Exam § T3 as an External-Dependency Drift Net](./scheduled-re-exam.mdx#t3-as-an-external-dependency-drift-net) |
| A broken link, missing page, dead image, or stale sitemap entry in the built site | [Built-Site Integrity Gates](./site-integrity-checks.mdx) |
| A visual diff regression across a refactor -- nothing catches it until someone eyeballs a screenshot | [Deterministic Visual Regression with Committed Baselines](./visual-regression-baselines.mdx) |
| It works on your machine but breaks on preview or production | [Environment-Tiered Testing Against Deployed Targets](./environment-tiered-testing.mdx) |

## Common Theme

Across all these projects, one theme emerges: **the testing approach must match the deployment target**. A web app needs browser-level testing. A CLI tool needs output verification. A Tauri app needs WebKit-specific testing. There is no universal test setup -- only appropriate test setups for specific contexts.
