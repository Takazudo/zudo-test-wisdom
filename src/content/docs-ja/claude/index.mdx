---
# Rationale (YAML comment — kept out of the MDX body so it is not indexed into the
# rendered page or the llms.txt / llms-full.txt feed):
# The EN /docs/claude/ index (src/content/docs/claude/index.mdx) is generated at build
# time by the claude-resources integration (plugins/claude-resources-plugin.mjs ->
# @takazudo/zudo-doc/integrations/claude-resources). That integration writes only into
# the default-locale docsDir (src/content/docs), so the generated claude / claude-md /
# claude-skills pages exist in English only. Without this stub, the JA route
# /ja/docs/claude/ falls back to the generated EN index, whose <CategoryNav> emits
# /ja/docs/claude-md/... links that 404 (the JA detail pages do not exist). This
# locale-only stub overrides that fallback and links to the English version instead.
# Mirrors zudo-doc's src/content/docs-ja/claude/index.mdx.
title: Claude
sidebar_position: 899
hide_sidebar: true
hide_toc: true
---

このセクションは英語（デフォルトロケール）でのみ提供されています。

[英語版の Claude カテゴリを見る](/docs/claude/)
